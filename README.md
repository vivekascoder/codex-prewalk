# codex-prewalk

Codex plugin for Prewalk-style model handoff: let a stronger planner inspect the repository and form an implementation plan, then hand execution to a cheaper/faster model after the first real workspace mutation.

Inspired by Stencil's Prewalk and the `pi-prewalk` implementation.
